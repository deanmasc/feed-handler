#include <iostream>
#include <network.h>

constexpr std::string MULTICAST_IP_ADDR {"224.0.0.1"};
constexpr int MULTICAST_PORT {30000};


int main() {
    std::cout << "Exchange process started" << std::endl;

    // First thing we want to do is create the multicast UDP socket


    return 0;
}
#include "order_book.h"

// OrderBook is defined entirely (inline) in order_book.h so it can be
// included and used by other translation units (e.g. the book manager).
// This file is kept as a translation unit for any future non-inline definitions.
